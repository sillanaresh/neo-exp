[phases.setup]
nixPkgs = ['python310', 'gcc', 'libgcc']

[phases.install]
cmds = [
    'pip install --upgrade pip setuptools wheel',
    'pip install --no-cache-dir -r requirements.txt'
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
